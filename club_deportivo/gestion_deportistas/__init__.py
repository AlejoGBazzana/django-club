"""Package for gestion_deportistas.

Keep this file minimal: avoid importing models here because Django
initialization imports app packages before the app registry is ready.
Move forms and other imports to separate modules (e.g. forms.py).
"""